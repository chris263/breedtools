% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augmented_row_column.R
\name{analyze_augmented_row_column_design}
\alias{analyze_augmented_row_column_design}
\title{Analyze an augmented row-column design}
\usage{
analyze_augmented_row_column_design(
  data,
  response,
  genotype_col = "all_entries",
  rowgroup_col = "rowgroup",
  colgroup_col = "colgroup",
  row_col = "row",
  col_col = "col",
  type_col = "type",
  control_col = "is_control"
)
}
\arguments{
\item{data}{A complete augmented row-column design data frame, usually
\code{design$design} from \code{augmented_row_column_design()}, with a response
column added.}

\item{response}{Name of the numeric response column.}

\item{genotype_col}{Name of the genotype/treatment column.}

\item{rowgroup_col}{Name of the row-group column.}

\item{colgroup_col}{Name of the column-group column.}

\item{row_col}{Name of the field row column.}

\item{col_col}{Name of the field column column.}

\item{type_col}{Optional entry type column. Used only for genotype summaries.}

\item{control_col}{Optional control flag column. Used only for genotype
summaries.}
}
\value{
A list with the analysis input, fitted model, model formula, ANOVA
table, formatted ANOVA table, plot-level fitted values and residuals,
genotype summaries, and model diagnostics.
}
\description{
Fits the fixed-effects model:
\code{y = mean + rowgroup + colgroup + rowgroup:colgroup + rowgroup:row + colgroup:col + genotype + error}.
The formatted ANOVA table reports degrees of freedom from augmented
row-column design formulas using field rows (\code{k}), field columns (\code{s}), row
groups (\code{gk}), column groups (\code{gs}), check cultivars (\code{vc}), and
unreplicated entries (\code{ve}).
}
\examples{
design <- augmented_row_column_design(
  treatments = paste0("G", seq_len(108)),
  controls = paste0("Check", seq_len(4)),
  rows_in_field = 12,
  cols_in_field = 12,
  rows_per_block = 4,
  cols_per_block = 4,
  n_candidates = 3,
  seed = 123
)

design_data <- design$design
design_data$yield <- rnorm(nrow(design_data), mean = 100, sd = 10)
analysis <- analyze_augmented_row_column_design(design_data, response = "yield")
analysis$anova

}
