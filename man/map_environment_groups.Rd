% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location_mapping.R
\name{map_environment_groups}
\alias{map_environment_groups}
\title{Create a map of environment groups}
\usage{
map_environment_groups(
  stratification_result,
  locations,
  ignore_case = FALSE,
  group_palette = NULL,
  ungrouped_color = "#7A7A7A",
  match_by = "location",
  strict = TRUE
)
}
\arguments{
\item{stratification_result}{Result from \code{run_environment_stratification()}
or \code{run_breedbase_environment_stratification()}.}

\item{locations}{Location coordinates from \code{read_locations()}, a location
file path, or a data frame with location, latitude, and longitude columns.}

\item{ignore_case}{Logical. If \code{TRUE}, location matching ignores case.}

\item{group_palette}{Optional vector of colors used for environment groups.}

\item{ungrouped_color}{Color for ungrouped environments.}

\item{match_by}{Name from the stratification result used to match the
\code{location} column in \code{locations}. Use \code{"location"} when coordinates are
named by location, \code{"environment"} when coordinates are named by the
constructed environment identifier, or \code{"environment_label"} when
coordinates are named by display labels.}

\item{strict}{Logical. If \code{TRUE}, stop when any environment location has no
matching coordinates.}
}
\value{
A \code{leaflet} htmlwidget.
}
\description{
Creates an interactive \code{leaflet} map where locations belonging to the same
environment group use the same marker color. Ungrouped environments are shown
with \code{ungrouped_color}.
}
\examples{
\dontrun{
result <- run_environment_stratification("phenotype.csv", trait = "yield")
locations <- read_locations("locations.csv", ignore_case = TRUE)
map_environment_groups(result, locations, ignore_case = TRUE)
}

}
