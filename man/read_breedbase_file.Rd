% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_breedbase_file.R
\name{read_breedbase_file}
\alias{read_breedbase_file}
\title{Read phenotype or design data from Excel, CSV, or tab-delimited files}
\usage{
read_breedbase_file(file, sheet = NULL, ...)
}
\arguments{
\item{file}{Path to the phenotype file.}

\item{sheet}{Optional Excel sheet name or number. If \code{NULL}, all non-empty
Excel sheets are combined.}

\item{...}{Additional arguments passed to \code{readxl::read_excel()} for Excel
files, \code{utils::read.csv()} for CSV files, or \code{utils::read.table()} for
tab-delimited files.}
}
\value{
A tibble containing the loaded phenotype data.
}
\description{
Loads phenotype or field-design data from \code{.xlsx}, \code{.xls}, \code{.csv}, \code{.tsv},
\code{.txt}, or \code{.design} files. Excel files are read from all non-empty sheets
and combined into one tibble with a \code{.sheet} column identifying the source
sheet. CSV files are comma-delimited; \code{.tsv}, \code{.txt}, and \code{.design} files are
tab-delimited.
}
\examples{
\dontrun{
pheno <- read_breedbase_file("breedbase_trials_pheno.xlsx")
pheno_csv <- read_breedbase_file("breedbase_trials_pheno.csv")
design <- read_breedbase_file("augmented_row_column.design")
}

}
