% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/location_mapping.R
\name{read_locations}
\alias{read_locations}
\title{Load location coordinates}
\usage{
read_locations(
  file,
  location_col = NULL,
  latitude_col = NULL,
  longitude_col = NULL,
  phenotype_data = NULL,
  phenotype_location_col = NULL,
  match_by = "location",
  reference_locations = NULL,
  ignore_case = FALSE,
  sheet = NULL,
  ...
)
}
\arguments{
\item{file}{Path to a location file, or an already loaded data frame.}

\item{location_col}{Optional location name column.}

\item{latitude_col}{Optional latitude column.}

\item{longitude_col}{Optional longitude column.}

\item{phenotype_data}{Optional raw phenotype data frame, prepared phenotype
data frame, or result from \code{run_environment_stratification()}. If supplied,
location names are extracted and checked against the location file.}

\item{phenotype_location_col}{Optional location column in \code{phenotype_data}.}

\item{match_by}{Column to extract when \code{phenotype_data} is a stratification
result. Use \code{"location"}, \code{"environment"}, or \code{"environment_label"}.}

\item{reference_locations}{Optional character vector of expected location
names. If supplied, every reference location must be present in \code{file}.}

\item{ignore_case}{Logical. If \code{TRUE}, location matching ignores case.}

\item{sheet}{Optional Excel sheet name or number passed to
\code{read_breedbase_file()}.}

\item{...}{Additional arguments passed to \code{read_breedbase_file()}.}
}
\value{
A tibble with \code{location}, \code{latitude}, and \code{longitude}.
}
\description{
Loads location metadata from a \code{.xlsx}, \code{.xls}, or \code{.csv} file, or from an
already loaded data frame. The returned table has standardized columns:
\code{location}, \code{latitude}, and \code{longitude}.
}
\examples{
locations <- data.frame(
  location = c("Loc1", "Loc2"),
  latitude = c(35.1, 36.2),
  longitude = c(-78.9, -79.4)
)

read_locations(locations)

}
